<h1>Rescue Mission</h1>
<h4><i>No need to be stranded with your questions anymore!</i></h3>

<div>
  <p>
    <%= render "form" %>
  </p>
  <p style= "color: red;">
    <% if @error %>
      <% @error.messages.each do |part, error| %>
        <% if error[0] %>
          <%= "The #{part} #{error[0]}. "%>
        <%end%>
      <%end%>
    <%end%>
    </p>
</div>

<div>
    <h3>Questions</h3>
  <ul>
    <% @questions.each do |q| %>
    <li><%= link_to q.title, question_path(q) %></li>
    </br>
    <%end%>
  </ul>
</div>
